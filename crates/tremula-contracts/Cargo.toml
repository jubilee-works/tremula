[package]
name = "tremula-contracts"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared file contracts between the tremula core and its language packs."

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
schemars = "1"

[lints]
workspace = true

[dev-dependencies]
jsonschema = { version = "0.49", default-features = false }
