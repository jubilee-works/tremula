{
  "about": "positions.py: normalized = normalize_replacement(replacement) → normalized = replacement",
  "model_resolved": "gpt-5.2-2025-12-11",
  "usage": {
    "prompt": 1128,
    "completion": 83,
    "total": 1211
  },
  "judgement": {
    "claim": "distinguishable",
    "witness": {
      "call": "validate_replacement(\"\\n\")",
      "expect_original": "Returns [] (because normalize_replacement converts a whitespace-only replacement into a valid empty replacement before parsing).",
      "expect_mutant": "Returns a non-empty list of errors (because parsing a bare newline as-is fails/produces a syntax issue)."
    }
  }
}
